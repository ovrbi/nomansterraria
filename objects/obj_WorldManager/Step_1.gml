/// @description Insert description here
// You can write your code in this editor
var xx = obj_player.x;
var yy = obj_player.y;
var begx = (xx - camera_get_view_width(view_camera[0]))div blockSize;
var endx = (xx + camera_get_view_width(view_camera[0]))div blockSize;
var begy = (yy - camera_get_view_height(view_camera[0]))div blockSize;
var endy = (yy + camera_get_view_height(view_camera[0]))div blockSize;

if (stx>endx){
	//for (var i = )
}